# 读取所有歌曲id
# 找到每一个歌曲的收藏用户
# 计算某一个歌曲与其他歌曲的相似度
# 相似度排序
# 取出前10位
# 插入数据
# Jaccard系数
# 交集元素个数 / 并集元素个数

传入的数据暂定为只有一个歌曲id

1. 根据歌曲id生成集合（同样收藏该歌曲的用户id）
    
2. 扫描整个数据中，并将所有歌曲的id相关联的用户id关联，生成一组集合 /// 时间过长
    考虑加快运算速度的方法
    快速生成两张pandas
3. 循环计算Jaccard系数，并将结果都加到每个歌曲id集合末尾
4. 排序
5. 获取前20，生成字典
6. 返回


